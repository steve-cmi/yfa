<h1>

  <% if @page.howto? -%>
  <div class="pull-right">
    <%= button_to "&laquo; Back to How To's".html_safe, howtos_path, :class => "btn btn-default" %>
  </div>
  <% end -%>

  <%= @page.title %>

</h1>

<hr />

<div class="row">

  <div class="col-md-6">
    <%= @page.content.html_safe %>
  </div>

  <div class="col-md-6">
    <%= image_tag @page.image.url(:show) if @page.image? %>
  </div>

</div>
